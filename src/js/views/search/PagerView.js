/*global define */
define(["jquery",
        "jqueryui",
        "underscore",
        "backbone"
    ],
function($, $ui, _, Backbone){

    "use strict";

    return Backbone.View.extend(
    /** @lends Backbone.View.prototype */ {

        render: function(){}

    });
});