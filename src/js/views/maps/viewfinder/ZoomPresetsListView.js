/**
 * @class ZoomPresetsListView
 * @classdesc Allow user to zoom to a preset location with certain data
 * layers enabled.
 * @classcategory Deprecated
 * @augments Backbone.View
 * @since 2.29.0
 * @deprecated since 0.0.0
 */

// Backward-compatibility alias. Use ViewfinderCardsListView instead.
define(["views/maps/viewfinder/ViewfinderCardsListView"], (
  ViewfinderCardsListView,
) => ViewfinderCardsListView);
