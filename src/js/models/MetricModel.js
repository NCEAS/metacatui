/*global define */
define(['jquery', 'underscore', 'backbone'],
    function($, _, Backbone) {
    'use strict';

    // Metric Model 
    // -------------
    var Metric = Backbone.Model.extend({
        defaults: {
            
        }

        initialize: function() {
            
        }

        getMetricData: function() {
            
        }
        
    });
    return Metric;
});